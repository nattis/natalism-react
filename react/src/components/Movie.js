const Movie = ({title, actor}) => {
    return (
        <>
            {
            <p>Title: {title} </p>,
            <p>Actor: {actor}</p>
            }
        </>
    );
     
};

export default Movie;